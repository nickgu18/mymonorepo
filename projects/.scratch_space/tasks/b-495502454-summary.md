Completed fixing PR #176 logic for extracting agent metadata without a global npm install.

- **Removed `npm install -g @google/gemini-cli`**: The worker no longer falls back to installing the published version just to read metadata.
- **Implemented `resolve_agent_metadata`**: Directly extracts the `VER` and `COM` variables from Harbor's shared agent cache or by executing a script within the built agent Docker container.
- **Fixed `cut` Syntax Error**: Addressed an invalid quoting issue in the `docker run` script (`cut -d"""` was failing due to lack of escaping inside the single quotes, fixed to `cut -d"\""`).
- **Propagated to BigQuery**: Updated `cli/cli.py`, `orchestrator/main.py`, `common/src/bench_hub_common/bigquery.py`, and `worker/sidecar_monitor.py` to seamlessly track `agent_version` and `agent_commit` fields for every job and instance.

The changes have been committed locally to your `main` branch.
