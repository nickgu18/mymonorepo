### Instruction

Review the provided @task, write rootcause to @analysis section, add your @fix_plan and all relevant files under @files.

### Task

write a script to get github repo information using gh cli

```
# GitHub CLI api
# https://cli.github.com/manual/gh_api

gh api \
  -H "Accept: application/vnd.github+json" \
  -H "X-GitHub-Api-Version: 2022-11-28" \
  /repos/OWNER/REPO
```

for repos in `/usr/local/google/home/guyu/Desktop/gcli/agent-prototypes/agent_prototypes/qabench/repos.csv`

then, update the repo.csv (add two more columns, if not already present) with 'creation_time' and 'star_cnt' (stargazers).

This script should be placed in `agent-prototypes/agent_prototypes/scripts` and is called
qabench_enrich_repos.py

Add this as a required step after adding repos in repo.csv in `agent-prototypes/agent_prototypes/qabench/qagen.md`

### analysis

The user wants to enrich the `repos.csv` file with additional data from GitHub. This requires a script that can read the CSV, call the GitHub API for each repository, and then write the new data back to the CSV. The user also wants to document this new script in the existing workflow.

### fix_plan

1.  Create a Python script `agent-prototypes/agent_prototypes/scripts/qabench_enrich_repos.py`.
2.  The script will use `pandas` to read and write the CSV file.
3.  The script will use the `subprocess` module to execute `gh api` commands to fetch repository data.
4.  The script will parse the JSON output from the API to extract the creation date and stargazer count.
5.  The script will add `creation_time` and `star_cnt` as new columns to the CSV file.
6.  Update the `agent-prototypes/agent_prototypes/qabench/qagen.md` file to include a new "Dataset Enrichment (Optional)" section that explains how to use the script.
7.  The script and the documentation have been created and updated as planned.

### files
- agent-prototypes/agent_prototypes/scripts/qabench_enrich_repos.py
- agent-prototypes/agent_prototypes/qabench/qagen.md