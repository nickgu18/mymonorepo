### Instruction

Review the provided @task, write rootcause to @analysis section, add your @fix_plan and all relevant files under @files.

### Task

`agent-prototypes/agent_prototypes/scripts/qabench_pipeline.py` has a series of configs that are passed through command line.
I want to update it so that it takes in a config_qabench_pipeline.yaml instead.

### analysis

The script `agent-prototypes/agent_prototypes/scripts/qabench_pipeline.py` uses `argparse` to handle command-line arguments. It is invoked by the `run_qabench_pipeline.sh` shell script, which passes the arguments to the python script.

### fix_plan

1.  Add the `pyyaml` dependency to the project using `poetry add pyyaml`.
2.  Create a `config_qabench_pipeline.yaml` file in the same directory as the script to hold the configuration.
3.  Modify `agent-prototypes/agent_prototypes/scripts/qabench_pipeline.py` to read the configuration from the YAML file instead of command-line arguments.
4.  Update `run_qabench_pipeline.sh` to accept the path to the config file and pass it to the python script.



### files
- `agent-prototypes/agent_prototypes/scripts/qabench_pipeline.py`
- `agent-prototypes/agent_prototypes/scripts/config_qabench_pipeline.yaml`
- `run_qabench_pipeline.sh`
- `agent-prototypes/pyproject.toml`