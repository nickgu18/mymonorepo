### Instruction

Review the provided @task, write rootcause to @analysis section, add your @fix_plan and all relevant files under @files.

### Task

agent-prototypes/experiments/qabench/2025-10-17/0-confident-ganguly/golden_set/temp.jsonl contains a list of questions and answer pairs in jsonl format. below is an example of a single entry

```json
{
    "instance_id": "wieslawsoltes__VelloSharp",
    "repo": "wieslawsoltes/VelloSharp",
    "base_commit": "d3208ba453156c390b5309244fbeae27b4a7b96c",
    "question_id": "eb0ac274",
    "question": "If the underlying GPU device is lost during a rendering operation (e.g., a driver crash or hardware removal), how is this error condition detected by the native Vello code and propagated back up to the managed C# layer? What specific exception type should my C# application code be prepared to catch, and does the library provide any mechanisms to attempt recovery?",
    "category": "Interface_Usage",
    "origin": "codebase_only",
    "synthesized_answer": "When the underlying GPU device is lost, the error is detected in the native Vello code and propagated to the C# layer as follows:\n\n1.  **Detection:** Vello's native `wgpu` backend detects a lost device when a call to `wgpu::Surface::get_current_texture` returns a `wgpu::SurfaceError::Lost`.\n\n2.  **Propagation:** The native error is converted into a `VelloStatus::RenderError` at the FFI boundary. The `VelloSharp` C# bindings check this status and throw a `System.InvalidOperationException` with a message like \"Surface render failed\" or \"Surface became invalid while rendering\".\n\nYour C# application code must be prepared to catch `System.InvalidOperationException` during rendering operations.\n\n**Recovery:**\n\nVello has a two-stage recovery process:\n\n1.  **Internal Recovery:** Upon detecting a lost surface, Vello's native layer transparently attempts to recover by recreating the swap chain. If this succeeds, no exception is thrown.\n2.  **Application Recovery:** If internal recovery fails, the `InvalidOperationException` is thrown. Your application should catch this exception and perform manual recovery by:\n    *   Disposing of the existing `VelloSurface` and `VelloSurfaceRenderer` objects.\n    *   Creating new instances of these objects.\n    *   Re-rendering the scene.",
    "source_synthesized_answer_file": "wieslawsoltes__VelloSharp/eb0ac274_answer_synthesized.json",
    "source_synthesized_verify_file": "wieslawsoltes__VelloSharp/eb0ac274_verify_result_synthesized.json"
}
```

In agent-prototypes/agent_prototypes/qabench/repos.csv, there are additional columns tied to a repo.

these were incldued in genearated answers as metadata agent-prototypes/experiments/qabench/2025-10-17/0-confident-ganguly/glowxq__glowxq-oj/4e5b3a33_answer_ans1.json


Now I want to incldue these metadate in generated golden_set entries, and populate these during golden qa collection step.

First, create a script to update temp.jsonl with metata from repo.csv.

Then, review qabench_pipeline on golden_qa collection, to include the data during the pipeline.

### analysis



### fix_plan




### files