---
name: task_tools
description: Creates a new task file from a template.
procedure: skill.sh
---

# Task Tools

This skill provides tools for managing tasks, specifically creating new task files from a template.

## Usage

Run the skill with an optional output directory argument:

```bash
./skill.sh [output_directory]
```

If no output directory is specified, it defaults to the skill's base directory.
